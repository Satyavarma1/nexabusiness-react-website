import React from 'react'
import { Link, useNavigate } from 'react-router-dom'
import { useState } from 'react'
export function Auth({ mode = 'login' }) { const [submitted, setSubmitted] = useState(false); const navigate = useNavigate(); const register = mode === 'register'; return <main className="auth-page"><div className="auth-panel"><p className="eyebrow">NexaCart / {register ? 'Join us' : 'Welcome back'}</p><h1>{register ? 'Make room for<br /><em>good things.</em>' : 'Come on in<br /><em>again.</em>'}</h1><p className="auth-copy">{register ? 'Create an account to save your favourites and keep track of every order.' : 'Sign in to see your saved pieces and order history.'}</p>{submitted ? <div className="auth-success"><h2>You’re all set.</h2><p>Welcome to the collection.</p><button className="button button-dark" onClick={() => navigate('/')}>Continue shopping</button></div> : <form className="auth-form" onSubmit={(event) => { event.preventDefault(); setSubmitted(true) }}>{register && <input required placeholder="Full name" aria-label="Full name" />}<input required type="email" placeholder="Email address" aria-label="Email address" /><input required type="password" placeholder="Password" aria-label="Password" /><button className="button button-dark" type="submit">{register ? 'Create account' : 'Sign in'}</button></form>}<p className="auth-switch">{register ? 'Already have an account? ' : 'New to NexaCart? '}<Link to={register ? '/login' : '/register'}>{register ? 'Sign in' : 'Create an account'}</Link></p></div><div className="auth-image"><img src="https://images.unsplash.com/photo-1549490349-8643362247b5?auto=format&fit=crop&w=1200&q=85" alt="Sculptural object in a warm, quiet interior" /></div></main> }
